import { createTheme } from '@mui/material/styles'

export const theme = createTheme({
  palette: {
    mode: 'dark',
    primary: {
      main: '#00ce46',
    },
    secondary: {
      main: '#ffffff',
    },
    background: {
      default: '#001e3c',
    },
  },
})

export default theme
